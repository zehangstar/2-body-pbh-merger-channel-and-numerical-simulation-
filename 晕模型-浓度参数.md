# 晕模型中的浓度参数、峰高与红移

本文是阅读 [《Simulating Binary Primordial Black Hole Mergers in Dark Matter Halos》](./Simulating%20Binary%20Primordial%20Black%20Hole%20Mergers%20in%20Dark%20Matter%20Halos.pdf) Section II 时，围绕浓度参数提出的问题与相应解释的整理。

主要问题是：

1. NFW 归一化中的 $\rho_{\rm crit}(z)$ 与浓度关系 $C(M,z)$ 中的 $z$，是否代表同一时刻？
2. 峰高 $\nu$ 只包含线性扰动信息，为什么能用来标记红移 $z$ 时质量为 $M$ 的非线性晕？
3. $\nu$ 中红移依赖的两种线性外推形式是什么？
4. Ludlow16 和 Prada12 的 $C(\nu)$ 是否只是模拟后的经验关系？浓度的真实依赖是否更加复杂？

## 1. 核心结论

在论文的实际计算中，同一个时间切片上的量应当一致：

$$
M(z),\qquad \rho_{\rm crit}(z),\qquad C[M(z),z].
$$

但是必须区分观察或计算红移 $z$ 与晕的形成红移 $z_{\rm form}$：

$$
z\equiv z_{\rm obs}\neq z_{\rm form}\quad\text{（一般情况下）}.
$$

峰高

$$
\nu(M,z)=\frac{\delta_{\rm sc}}{\sigma(M,z)}
$$

衡量的是：质量尺度 $M$ 的线性涨落截至红移 $z$ 达到坍缩条件的稀有程度。它不是单个晕的年龄，也不唯一决定单个晕的浓度。

因此，$C(\nu)$ 最准确的理解是晕族群的统计关系：

$$
\operatorname{median}[C\mid M,z]
\simeq \bar C[\nu(M,z)],
$$

而不是基本定律

$$
C_i=C(\nu_i)
$$

对每一个晕都严格成立。

## 2. $\rho_{\rm crit}(z)$ 与 $C(M,z)$ 中的红移

### 2.1 在一次晕结构计算中，它们是同一个 $z$

宇宙临界密度为

$$
\rho_{\rm crit}(z)=\frac{3H^2(z)}{8\pi G}.
$$

若晕质量采用 $M_{200c}$ 或论文中的相同定义，则 virial 半径满足

$$
M(z)=\frac{4\pi}{3}R_{\rm vir}^3(z)\,
200\rho_{\rm crit}(z).
$$

所以

$$
R_{\rm vir}(z)=
\left[
\frac{3M(z)}{4\pi\,200\rho_{\rm crit}(z)}
\right]^{1/3}.
$$

在同一红移使用浓度关系

$$
C(z)=C[M(z),z],
$$

随后得到

$$
R_s(z)=\frac{R_{\rm vir}(z)}{C[M(z),z]},
$$

以及

$$
\rho_s(z)=\rho_{\rm crit}(z)
\frac{200C^3[M(z),z]}{3g(C[M(z),z])},
$$

其中

$$
g(C)=\ln(1+C)-\frac{C}{1+C}.
$$

因此，计算 $z$ 时刻晕结构时，$M$、$\rho_{\rm crit}$ 和 $C$ 必须取自同一时间切片。

### 2.2 观察红移与形成红移不同

$C(M,z)$ 描述的是：

> 在红移 $z$ 的快照中，已经存在且当时质量为 $M$ 的晕族群的典型浓度。

它不是：

> 恰好在红移 $z$ 刚刚坍缩形成、质量为 $M$ 的晕的浓度。

一个在 $z$ 时质量为 $M$ 的晕，其核心可能在更早的

$$
z_{\rm form}>z
$$

形成，随后通过平滑吸积和并合增长到当前质量。

Ludlow 模型还使用特征形成红移 $z_{-2}$。它大致表示主祖先或 collapsed mass history 达到尺度半径内部质量 $M_{-2}=M(<r_{-2})$ 的时刻。模型建立的物理关联是

$$
\langle\rho\rangle(<r_{-2})
\propto \rho_{\rm crit}(z_{-2}).
$$

这里的 $z_{-2}$ 是形成史变量，不应与测量晕结构时的 $z$ 混为一谈。

另一方面，

$$
\rho_s(z)=\delta_{\rm NFW}(C)\rho_{\rm crit}(z)
$$

是由当前晕的 $M_{200c}$ 定义和 NFW 质量归一化得到的代数关系。这两个公式中的临界密度承担不同角色。

## 3. 峰高参数的来由

### 3.1 在拉格朗日尺度上平滑线性密度场

线性密度对比度定义为

$$
\delta(\mathbf x,z)=
\frac{\rho(\mathbf x,z)-\bar\rho_m(z)}{\bar\rho_m(z)}.
$$

为了讨论质量尺度 $M$，需要用窗口函数在拉格朗日共动尺度 $R_L$ 上平滑：

$$
\delta_R(\mathbf x,z)=
\int d^3x'\,
W_R(|\mathbf x-\mathbf x'|)\delta(\mathbf x',z).
$$

$R_L$ 与质量的关系为

$$
M=\frac{4\pi}{3}\rho_{m,0}R_L^3.
$$

$R_L$ 是组成该晕的物质在坍缩前、按平均宇宙密度计算所占据的尺度，并不是非线性晕的 $R_{\rm vir}$。

平滑场的方差为

$$
\sigma^2(M,z)=
\langle\delta_R^2\rangle
=\int_0^\infty\frac{k^2dk}{2\pi^2}
P_{\rm lin}(k,z)|W(kR_L)|^2.
$$

一般来说：

- 大质量对应大平滑尺度，$\sigma(M)$ 较小；
- 小质量对应小平滑尺度，保留的小尺度功率更多，$\sigma(M)$ 较大。

$\sigma(M,z)$ 描述的是质量尺度 $M$ 上线性涨落的统计强度，不是某一个具体晕的密度。

### 3.2 球对称坍缩阈值

在球对称坍缩模型中，当一个过密区域发生非线性坍缩时，若仍用线性理论外推同一个扰动，其密度对比度达到

$$
\delta_{\rm sc,0}\simeq1.686.
$$

因此，在红移 $z$ 判断某质量尺度是否已经坍缩，可以写成

$$
\delta_R(M,z)\geq\delta_{\rm sc,0}.
$$

它的含义是：

- 等号：理想球对称区域恰好在 $z$ 坍缩；
- 大于阈值：理想模型中应当在 $z$ 之前就已坍缩；
- 小于阈值：截至 $z$ 尚未达到坍缩条件。

因此，固定红移的 Press–Schechter 条件首先对应“截至 $z$ 已坍缩”，而不是严格意义上的“恰好在 $z$ 新形成”。

### 3.3 用标准差衡量阈值高度

线性密度场近似服从高斯分布：

$$
p(\delta_R)d\delta_R=
\frac{1}{\sqrt{2\pi}\sigma}
\exp\left(-\frac{\delta_R^2}{2\sigma^2}\right)d\delta_R.
$$

于是自然定义峰高

$$
\boxed{
\nu(M,z)=\frac{\delta_{\rm sc,0}}{\sigma(M,z)}
}.
$$

它表示坍缩阈值位于高斯分布均值之上多少个标准差：

- $\nu\sim1$：典型涨落；
- $\nu\gg1$：非常罕见的高峰；
- 同一红移下，大质量晕通常有较大的 $\nu$。

Press–Schechter 理论中，截至 $z$ 坍缩到大于质量 $M$ 的质量分数近似为

$$
F(>M,z)=
\operatorname{erfc}\left(\frac{\nu(M,z)}{\sqrt2}\right).
$$

特征非线性质量 $M_\star(z)$ 定义为

$$
\nu(M_\star,z)=1.
$$

## 4. 线性峰为什么能标记非线性晕

这里采用了一个统计映射：

$$
\text{红移 }z\text{ 时质量为 }M\text{ 的非线性晕}
\longleftrightarrow
\text{初始场中拉格朗日质量为 }M\text{ 的区域}.
$$

在 $N$-body 模拟中，可以选出红移 $z$ 时属于某个质量为 $M$ 的晕的粒子，再把这些粒子追溯到初始条件。这些物质在初始场中占据一个拉格朗日区域，其总质量仍为 $M$。因此可以用 $M$ 所对应的 $\sigma(M)$ 和 $\nu(M,z)$ 给这个晕赋予一个“初始峰稀有度”的统计标签。

但是，这种映射不意味着：

- 该晕的全部质量在同一时刻坍缩；
- 该晕的核心恰好在观察红移 $z$ 形成；
- 相同 $\nu$ 的晕具有完全相同的吸积历史；
- 线性理论能预测单个晕的全部内部结构。

更准确地说，$\nu$ 是对晕初始条件与典型演化阶段的低维统计描述。

## 5. “晕丰度”不等于“晕形成率”

固定红移的晕质量函数

$$
\frac{dn(M,z)}{dM}
$$

描述的是红移 $z$ 的宇宙快照中存在多少质量为 $M$ 的晕。

它不是晕形成率

$$
\frac{d^2n_{\rm form}}{dM\,dt}.
$$

原因是一个质量区间内晕的数量同时受以下过程影响：

1. 新晕进入该质量区间；
2. 晕通过吸积离开该质量区间；
3. 已有晕互相并合成为更大晕；
4. 某些晕成为更大系统的子晕。

因此

$$
\frac{\partial}{\partial t}\frac{dn}{dM}
$$

只是形成、增长和并合消失共同造成的净变化，不能直接解释为纯形成率。

要计算形成时间分布和祖先质量分布，需要 excursion-set 的条件首次穿越问题或 merger tree，而不能只使用单个 $\nu(M,z)$。

## 6. Excursion-set 的首次穿越图像

定义

$$
S(M)=\sigma^2(M).
$$

不断减小平滑尺度时，通常有

$$
R_L\downarrow
\quad\Longleftrightarrow\quad
M\downarrow
\quad\Longleftrightarrow\quad
S\uparrow.
$$

对初始场的每一个位置，平滑密度 $\delta(S)$ 可以看作随 $S$ 变化的随机游走。在给定红移设置坍缩障碍

$$
B(z)=\delta_{\rm sc}(z).
$$

随机游走第一次穿越障碍的位置 $S(M)$，被用于给该位置分配红移 $z$ 时所属晕的质量。这一“首次穿越”条件处理了 cloud-in-cloud 问题，即一个已经坍缩的小区域可能位于一个更大的坍缩区域中。

在把密度场统一外推到今天的约定下：

- 高红移时 $B(z)$ 较高，通常只形成较小的祖先结构；
- 随宇宙演化，$B(z)$ 降低，首次穿越对应的典型质量增大；
- 这在统计上描述了晕通过吸积和并合增长。

因此，某个晕在红移 $z$ 的当前质量并不等于它最早形成时的质量。

## 7. 峰高的两种等价线性外推形式

线性扰动按照增长因子演化：

$$
\delta_L(\mathbf x,z)=D(z)\delta_L(\mathbf x,0),
\qquad D(0)=1.
$$

因此

$$
P_{\rm lin}(k,z)=D^2(z)P_{\rm lin}(k,0),
$$

以及

$$
\sigma(M,z)=D(z)\sigma(M,0).
$$

同一个坍缩条件可以用两种等价方式书写。

| 约定 | 使用的方差 | 坍缩阈值 | 峰高 |
|---|---|---|---|
| A：将扰动演化到 $z$ | $\sigma(M,z)=D(z)\sigma(M,0)$ | $\delta_{\rm sc,0}\simeq1.686$ | $\nu=1.686/\sigma(M,z)$ |
| B：将扰动外推到今天 | $\sigma(M,0)$ | $\delta_{\rm sc}(z)=1.686/D(z)$ | $\nu=\delta_{\rm sc}(z)/\sigma(M,0)$ |

### 7.1 约定 A：固定障碍，演化扰动

在红移 $z$ 直接评价线性密度场：

$$
\delta_L(M,z)\geq1.686.
$$

相应地

$$
\boxed{
\nu(M,z)=\frac{1.686}{\sigma(M,z)}
=\frac{1.686}{D(z)\sigma(M,0)}
}.
$$

### 7.2 约定 B：固定今天的扰动场，移动障碍

把所有初始扰动线性外推到今天。一个区域若要在红移 $z$ 前坍缩，需要满足

$$
D(z)\delta_L(M,0)\geq1.686,
$$

也就是

$$
\delta_L(M,0)\geq
\frac{1.686}{D(z)}
\equiv\delta_{\rm sc}(z).
$$

因此

$$
\boxed{
\nu(M,z)=
\frac{\delta_{\rm sc}(z)}{\sigma(M,0)}
=\frac{1.686}{D(z)\sigma(M,0)}
}.
$$

二者完全等价：

$$
\frac{1.686}{\sigma(M,z)}
=\frac{1.686}{D(z)\sigma(M,0)}
=\frac{\delta_{\rm sc}(z)}{\sigma(M,0)}.
$$

在 Einstein–de Sitter 或高红移物质主导近似下，

$$
D(z)\simeq\frac{1}{1+z},
$$

所以

$$
\delta_{\rm sc}(z)
=\frac{1.686}{D(z)}
\simeq1.686(1+z).
$$

一般 $\Lambda$CDM 宇宙应使用真实的 $D(z)$，而不是始终把 $1/(1+z)$ 当作精确结果。

### 7.3 不能重复计算增长因子

不能同时使用

$$
\delta_{\rm sc}(z)=\frac{1.686}{D(z)}
$$

和

$$
\sigma(M,z)=D(z)\sigma(M,0)
$$

再定义

$$
\nu=\frac{\delta_{\rm sc}(z)}{\sigma(M,z)}.
$$

这样会得到

$$
\nu_{\rm wrong}
=\frac{1.686}{D^2(z)\sigma(M,0)},
$$

相当于把线性增长计算了两次。

本文写道

$$
\nu(M,z)=\frac{\delta_{\rm sc}(z)}{\sigma(M,z)},
\qquad
\delta_{\rm sc}(z)=1.686(1+z),
$$

记号存在含混。物理上一致的理解只能是以下二者之一：

$$
\nu(M,z)=\frac{1.686(1+z)}{\sigma(M,0)}
$$

或

$$
\nu(M,z)=\frac{1.686}{\sigma(M,z)}.
$$

使用 Ludlow16 或 Prada12 的具体公式时，应严格采用原模型对 $\sigma$、$D(z)$ 和坍缩阈值的定义。

## 8. Ludlow16 模型：从形成史理论到 Appendix C 快速拟合

在写代码前，必须区分 Ludlow16 的两个层次：

1. **底层物理模型**：用晕的 collapsed mass history（CMH）和特征形成时刻预测浓度；
2. **Appendix C 快速拟合**：在 Planck CDM 宇宙学下，把底层模型的数值结果压缩成一组容易计算的闭式公式。

我们复现 PBH 论文 Fig. 1–2 时使用的是第二层快速拟合，但理解第一层才能知道公式中的量各自代表什么。

### 8.1 先避免两个字母 $C$ 的冲突

PBH 论文用大写 $C$ 表示 NFW 浓度，而 Ludlow16 原文通常写作小写 $c$：

$$
c\equiv\frac{r_{200}}{r_{-2}}.
$$

对 NFW 剖面，$r_{-2}=r_s$，即密度对数斜率为 $-2$ 的尺度半径。

Ludlow16 底层模型还用另一个常数 $\mathcal C$ 表示“内部特征密度与形成时刻临界密度之间的比例”。它不是浓度。为避免混淆，下面把这个比例常数改记为 $A_\rho$：

$$
\langle\rho_{-2}\rangle=A_\rho\rho_{\rm crit}(z_{-2}).
$$

因此本文后面统一采用：

- $c$：halo concentration；
- $A_\rho$：Ludlow 形成密度关系的归一化常数；
- $g(c)=\ln(1+c)-c/(1+c)$：NFW 质量积分函数。

### 8.2 第一步：由浓度确定尺度半径内的质量

这一关系依赖所采用的密度剖面。Ludlow16 的底层模型和 Appendix C 标定采用形状参数 $\alpha=0.18$ 的 Einasto 剖面；PBH 论文则采用 NFW 剖面。两者都可以用

$$
c=\frac{r_{200}}{r_{-2}}
$$

定义浓度，但给定 $c$ 时的内部质量比例 $M_{-2}/M_{200}$ 并不完全相同。

先定义一般的剖面质量比例函数

$$
F_{-2}(c)\equiv\frac{M(<r_{-2})}{M_{200}}.
$$

由于

$$
M_{200}=\frac{4\pi}{3}200\rho_{\rm crit}(z_0)r_{200}^3,
\qquad r_{200}=cr_{-2},
$$

任何球对称剖面都满足

$$
\boxed{
\frac{\langle\rho_{-2}\rangle}{\rho_{\rm crit}(z_0)}
=200c^3F_{-2}(c)
}.
$$

下面再看与 PBH 论文直接配套的 NFW 特例。

NFW 晕在半径 $r$ 内的质量为

$$
M(<r)=4\pi\rho_s r_s^3g(r/r_s).
$$

在 $r_{200}=cr_s$ 处，

$$
M_{200}=4\pi\rho_s r_s^3g(c).
$$

在 $r_{-2}=r_s$ 处，

$$
M_{-2}\equiv M(<r_{-2})
=4\pi\rho_s r_s^3g(1).
$$

所以

$$
\boxed{
\frac{M_{-2}}{M_{200}}=\frac{g(1)}{g(c)}
}.
$$

尺度半径内的平均密度定义为

$$
\langle\rho_{-2}\rangle
=\frac{M_{-2}}{(4\pi/3)r_{-2}^3}.
$$

代入上面的一般关系可得

$$
\boxed{
\frac{\langle\rho_{-2}\rangle}{\rho_{\rm crit}(z_0)}
=200c^3\frac{g(1)}{g(c)}
}.
$$

这说明只要选定密度剖面并知道浓度 $c$，就知道晕内部特征密度相对于观察时刻临界密度的大小。

需要牢记：

- $F_{-2}(c)=g(1)/g(c)$ 是 NFW 的结果；
- Ludlow16 的原始底层计算对 Einasto 剖面计算 $F_{-2}(c)$；
- Appendix C 已经把其 fiducial Einasto 模型的结果吸收到浓度拟合系数中；
- PBH 论文取得 Ludlow16 的 $c(M,z)$ 后再建立 NFW 晕，是一种常用但并非逐项完全一致的模型组合。

### 8.3 第二步：定义 collapsed mass history

只看最主要祖先的质量增长史，有时不能很好地描述 WDM 等具有截断功率谱的模型。Ludlow16 因而定义 collapsed mass history：

$$
M_{\rm coll}(z;f)
=\sum_{i:\,M_i(z)>fM_0}M_i(z).
$$

这里：

- $M_0$ 是晕在被识别时刻 $z_0$ 的质量；
- $M_i(z)$ 是红移 $z$ 时各个祖先晕的质量；
- 只统计满足 $M_i(z)>fM_0$ 的祖先；
- Ludlow16 的基准选择是 $f=0.02$。

因此 $M_{\rm coll}$ 不是单个主祖先的质量，而是所有足够大的祖先结构的总质量。

### 8.4 第三步：定义特征形成红移 $z_{-2}$

Ludlow16 把 $z_{-2}$ 定义为：collapsed mass history 第一次达到当前晕尺度半径内部质量 $M_{-2}$ 的时刻，亦即

$$
\boxed{
M_{\rm coll}(z_{-2};f)=M_{-2}
}.
$$

这一定义不是说整个 $M_{200}$ 晕在 $z_{-2}$ 已经形成，而是说构成其内区特征质量的物质，在那时已经进入一批质量不小于 $fM_0$ 的坍缩祖先中。

扩展 Press–Schechter 理论给出的平均关系可写为

$$
\frac{M_{-2}}{M_0}
=\operatorname{erfc}\!\left[
\frac{\delta_{\rm sc}(z_{-2})-\delta_{\rm sc}(z_0)}
{\sqrt{2\left[\sigma^2(fM_0)-\sigma^2(M_0)\right]}}
\right].
$$

它把特征形成时刻与线性功率谱信息 $\sigma(M)$ 联系起来。

### 8.5 第四步：内部密度记住形成时刻的宇宙密度

模拟显示，尺度半径内部的平均密度与 $z_{-2}$ 时的宇宙临界密度近似成正比：

$$
\boxed{
\langle\rho_{-2}\rangle
=A_\rho\rho_{\rm crit}(z_{-2})
}.
$$

这条关系表达了 Ludlow 模型的核心物理图像：

$$
\text{内区较早组装}
\Rightarrow
\rho_{\rm crit}(z_{-2})\text{ 较高}
\Rightarrow
\text{内部特征密度较高}
\Rightarrow
c\text{ 较大}.
$$

直接用模拟 merger tree 构造 CMH 时，Ludlow16 得到 $A_\rho\simeq400$。当使用简化的 EPS 解析 CMH 时，因为球对称坍缩近似并不精确，为了使平均浓度关系匹配模拟，采用 $A_\rho=650$。我们复现 Appendix C 拟合时不需要手动代入这两个数，因为它们的效果已经包含在拟合系数中。

### 8.6 底层模型如何求出浓度

给定 $M_0,z_0$、线性功率谱、密度剖面以及 $f,A_\rho$ 后，底层模型联立求解：

$$
\frac{M_{-2}}{M_0}=F_{-2}(c),
$$

$$
M_{\rm coll}(z_{-2};f)=M_{-2},
$$

$$
200c^3F_{-2}(c)
=A_\rho\frac{\rho_{\rm crit}(z_{-2})}{\rho_{\rm crit}(z_0)}.
$$

未知量主要是 $c$ 和 $z_{-2}$。这一步通常需要数值求根，并不等同于简单地把 $\nu$ 代入一个基本定律。

若为了与 PBH 论文一致而采用 NFW 剖面，才进一步令

$$
F_{-2}(c)=\frac{g(1)}{g(c)}.
$$

### 8.7 Appendix C 为什么还要给出快速拟合

每次都生成 CMH 并联立求根很麻烦。Ludlow16 因此针对 Planck CDM 宇宙学，把底层模型产生的 $c(M,z)$ 结果拟合成 Appendix C 的闭式公式：

$$
c(\nu,z)=c_0
\left(\frac{\nu}{\nu_0}\right)^{-\gamma_1}
\left[
1+\left(\frac{\nu}{\nu_0}\right)^{1/\beta}
\right]^{-\beta(\gamma_2-\gamma_1)}.
$$

这是一个平滑破幂律：

- $c_0$ 控制归一化；
- $\nu_0$ 给出两段幂律转换的大致位置；
- $\gamma_1$ 是低 $\nu$ 端的渐近斜率；
- $\gamma_2$ 是高 $\nu$ 端的渐近斜率；
- $\beta$ 控制两段之间的过渡宽度。

这些参数仍然显式依赖红移：

$$
c_0=3.395(1+z)^{-0.215},
$$

$$
\beta=0.307(1+z)^{0.540},
$$

$$
\gamma_1=0.628(1+z)^{-0.047},
$$

$$
\gamma_2=0.317(1+z)^{-0.893},
$$

以及

$$
\nu_0=\left(
4.135-0.564a^{-1}-0.210a^{-2}
+0.0557a^{-3}-0.00348a^{-4}
\right)D^{-1}(z),
$$

其中

$$
a=(1+z)^{-1}.
$$

所以 Appendix C 更准确地说是

$$
c=c(\nu,z),
$$

并不是完全没有残余红移依赖的严格普适 $c(\nu)$。

### 8.8 $\xi$ 到底是什么

峰高需要

$$
\nu(M,z)=\frac{1.686}{\sigma(M,z)},
$$

而严格的 $\sigma$ 来自功率谱积分：

$$
\sigma^2(M,z)=\frac{1}{2\pi^2}
\int_0^\infty P(k,z)W^2(k,M)k^2dk.
$$

为了复现浓度关系，若每次都计算这个积分，就还要实现转移函数、功率谱归一化、top-hat 窗函数和数值积分。Appendix C 针对固定的 Planck CDM 功率谱，直接把积分结果近似为

$$
\boxed{
\sigma(M,z)=D(z)
\frac{22.26\,\xi^{0.292}}
{1+1.53\xi^{0.275}+3.36\xi^{0.198}}
},
$$

其中

$$
\boxed{
\xi\equiv
\left(
\frac{M}{10^{10}h^{-1}M_\odot}
\right)^{-1}
}.
$$

$\xi$ 的性质是：

1. 它只是无量纲的逆质量变量；
2. 它不是峰高 $\nu$；
3. 它不是密度对比度，也不是形成红移；
4. 它没有引入新的晕物理；
5. $10^{10}h^{-1}M_\odot$ 是拟合时选择的参考质量，用来使变量大小和系数便于处理；
6. 取质量的倒数，使小质量对应大 $\xi$，从而可以用若干正的分数幂拟合 CDM 功率谱产生的 $\sigma(M)$ 曲线。

因此，逻辑顺序不是

$$
\xi\longrightarrow\text{新的物理机制},
$$

而是

$$
P(k)\text{ 的积分结果}
\longrightarrow\sigma(M,0)
\longrightarrow
\text{用 }\xi\text{ 写成一个方便的数值拟合}.
$$

换言之，$\xi$ 是计算工具，不是物理状态量。

### 8.9 质量单位为什么必须先乘 $h$

我们的 Python 函数输入物理质量 $M_{\rm phys}$，单位为 $M_\odot$。原式却以 $h^{-1}M_\odot$ 为单位。

若

$$
M_{\rm phys}=Xh^{-1}M_\odot,
$$

则

$$
X=h\frac{M_{\rm phys}}{M_\odot}.
$$

例如 Ludlow16 采用 $h=0.678$，对于

$$
M_{\rm phys}=10^{12}M_\odot,
$$

以 $h^{-1}M_\odot$ 表示的数值为

$$
M_h=0.678\times10^{12}=6.78\times10^{11}.
$$

于是

$$
\xi=left(\frac{6.78\times10^{11}}{10^{10}}\right)^{-1}
=\frac{1}{67.8}
\simeq0.01475.
$$

这里容易犯的错误是直接使用

$$
(10^{12}/10^{10})^{-1}=0.01,
$$

这会漏掉 $h$ 的单位转换。

### 8.10 从质量到浓度的完整计算链

使用 Appendix C 快速拟合时，程序应按以下顺序执行：

$$
M[M_\odot]
\xrightarrow{\times h}
M[h^{-1}M_\odot]
\longrightarrow\xi(M)
\longrightarrow\sigma(M,z)
\longrightarrow\nu(M,z)
\longrightarrow c(\nu,z).
$$

逐步写开就是：

1. 将物理质量转换为以 $h^{-1}M_\odot$ 表示的数值；
2. 计算无量纲逆质量 $\xi$；
3. 用 $\xi$ 的拟合式得到 $\sigma(M,0)$；
4. 乘增长因子 $D(z)$ 得到 $\sigma(M,z)$；
5. 计算峰高 $\nu=1.686/\sigma$；
6. 计算 $c_0,\beta,\gamma_1,\gamma_2,\nu_0$；
7. 代入平滑破幂律得到浓度。

其中只有第 2 步的 $\xi$ 是为了书写拟合公式而引入；真正具有直接统计物理意义的是 $\sigma$ 和 $\nu$。

### 8.11 拟合范围与本文的外推

Ludlow16 Appendix C 给出的适用范围约为

$$
-8\leq\log_{10}\left(\frac{M}{h^{-1}M_\odot}\right)\leq16.5,
$$

以及

$$
0\leq\log_{10}(1+z)\leq1,
$$

即大致 $0\leq z\leq9$。PBH 论文将曲线画到 $z=12$，因此 $9<z\leq12$ 的部分属于对拟合公式的短距离外推，而不是 Ludlow16 明确校准的范围。

## 9. 为什么 $C$ 可以近似写成 $C(\nu)$

$\nu$ 本身不会通过线性理论直接产生 NFW 浓度。真正的逻辑是

$$
\nu
\longrightarrow
\text{质量尺度的稀有程度}
\longrightarrow
\text{典型形成和吸积历史}
\longrightarrow
\text{典型内部密度}
\longrightarrow
\bar C.
$$

因此，并不是

$$
\nu\overset{\text{线性理论}}{\Longrightarrow}C,
$$

而是

$$
\nu
+\text{形成史模型或 }N\text{-body 校准}
\Longrightarrow\bar C.
$$

更完整的描述应是条件概率分布

$$
P(C\mid M,z,\text{cosmology},\text{halo definition},\ldots),
$$

其平均或中位趋势在一定范围内可以压缩成

$$
\bar C(M,z)\simeq C[\nu(M,z)].
$$

## 10. Prada12 与 Ludlow16 的性质

### 10.1 Prada12：主要是模拟经验拟合

Prada12 使用多组大型 $N$-body 模拟：

1. 在各个红移识别暗物质晕；
2. 测量晕的浓度或 $V_{\max}/V_{200}$；
3. 根据质量、红移和线性功率谱计算 $\sigma$ 或 $\nu$；
4. 发现不同质量和红移的数据用 $\sigma$ 表示后接近一条普适曲线；
5. 给出模拟校准的拟合公式。

Prada12 的 $C(\nu)$ 因而主要是经验关系。其高质量端的上翘还可能受到快速吸积、未弛豫晕和浓度测量方式影响。

### 10.2 Ludlow16：有形成史解释的半解析模型

Ludlow16 的基础思想是

$$
\text{collapsed mass history}
\longleftrightarrow
\text{晕内部质量剖面}.
$$

它利用特征形成时刻与晕内部尺度密度之间的关系来预测浓度。该模型比单纯拟合 $C(M,z)$ 更有物理动机，但其中的比例常数、形成史近似和最终关系仍然经过 $N$-body 模拟验证与校准。

因此 Ludlow16 应归类为

$$
\boxed{
\text{有形成史物理解释的半解析、模拟校准模型}
}.
$$

在标准 CDM 中，典型质量吸积史很大程度上可由峰高组织，因此 Ludlow 模型最终也能近似表现为 $C(\nu)$。但这不表示峰高包含了每个晕的完整形成史。

## 11. 浓度本质上依赖更多变量

对于单个晕，更合理的概念表达是

$$
C_i=C\!\left[
M,z,
\text{质量吸积史},
\text{形成时间},
\text{近期并合},
\text{环境},
\text{动力学状态},
\text{功率谱形状},
\text{晕定义},
\text{重子过程},\ldots
\right].
$$

固定 $M$ 和 $z$ 时，不同晕仍然具有不同浓度，原因包括：

- 形成时间和吸积历史不同；
- 最近是否经历过大并合；
- 是否已经达到动力学弛豫；
- 潮汐和大尺度环境不同；
- 自旋、三轴性和子结构不同；
- 浓度的测量方法和晕边界定义不同；
- 包含重子时，冷却、恒星反馈和活动星系核反馈会改变中心密度。

因此 $C(\nu)$ 的“普适性”是有限的，通常隐含：

- 讨论族群平均或中位浓度；
- 采用特定的晕质量和边界定义；
- 主要针对无碰撞暗物质模拟；
- 限于模型校准的质量、红移和宇宙学范围；
- 对是否选择弛豫晕有特定规定。

若功率谱存在 WDM 截断或其他特征，仅使用 $\nu$ 往往不能完整描述浓度，因为局部功率谱形状和祖先结构的数量也会改变质量吸积史。Ludlow16 因此强调完整的 collapsed mass history，而不只是单个峰高。

## 12. 本文采用的近似

本文并未逐个模拟暗物质晕从初始扰动到非线性形成的全过程，而是采用以下流程：

$$
M_0
\longrightarrow M(z)
\longrightarrow C[M(z),z]
\longrightarrow
R_{\rm vir}(z),R_s(z),\rho_s(z).
$$

其中质量吸积史写成

$$
M(z)=M_0(1+z)^\alpha e^{\beta z}.
$$

然后作者在每个红移上调用 Ludlow16 或 Prada12，为当时质量为 $M(z)$ 的晕指定一个代表性浓度。

这意味着论文考虑了平均质量增长和平均浓度演化，但没有显式包含：

- 固定 $M,z$ 下的浓度散布；
- 每个晕独有且时间相关的并合史；
- 大并合后的短期非平衡结构；
- 环境导致的 assembly bias；
- 浓度演化轨迹之间的个体差异。

从统计角度，它相当于把一条平均质量吸积轨迹 $M(z)$ 与各红移的族群平均关系 $\bar C(M,z)$ 拼接起来。

## 13. 最终物理图像

可以把相关概念分成三个层次。

### 线性初始条件层

$$
P_{\rm lin}(k)
\longrightarrow\sigma(M,z)
\longrightarrow\nu(M,z).
$$

$\nu$ 衡量质量尺度达到坍缩条件的稀有程度。

### 非线性形成史层

$$
\nu+\text{环境和随机性}
\longrightarrow
\text{形成时间与质量吸积史}.
$$

相同 $\nu$ 的晕仍可具有不同历史。

### 晕结构层

$$
\text{质量吸积史与动力学演化}
\longrightarrow
r_s,\rho_s,C.
$$

模拟表明，在标准 CDM 晕族群中，前两层的大部分平均依赖可以压缩成

$$
\bar C(M,z)\simeq C[\nu(M,z)].
$$

最终应记住：

$$
\boxed{
\nu\text{ 控制浓度的主要统计趋势，但不能唯一决定单个晕的浓度。}
}
$$

$$
\boxed{
C(\nu)\text{ 是模拟校准的族群关系；浓度本质上由形成史和动力学演化共同决定。}
}
$$

## 参考文献

1. Aljaf & Cholis, *Simulating Binary Primordial Black Hole Mergers in Dark Matter Halos*, [arXiv:2408.06515](https://arxiv.org/abs/2408.06515).
2. Prada et al., *Halo concentrations in the standard $\Lambda$ cold dark matter cosmology*, [MNRAS 423, 3018 (2012)](https://doi.org/10.1111/j.1365-2966.2012.21007.x).
3. Ludlow et al., *The mass–concentration–redshift relation of cold dark matter haloes*, [MNRAS 441, 378 (2014)](https://doi.org/10.1093/mnras/stu483).
4. Ludlow et al., *The mass–concentration–redshift relation of cold and warm dark matter haloes*, [MNRAS 460, 1214 (2016)](https://doi.org/10.1093/mnras/stw1046).
5. Ludlow & Porciani, *The peaks formalism and the formation of cold dark matter haloes*, [MNRAS 413, 1961 (2011)](https://doi.org/10.1111/j.1365-2966.2011.18282.x).
