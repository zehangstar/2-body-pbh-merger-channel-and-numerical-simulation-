可以。先把论文第一部分理解成一条“从宇宙学晕到 PBH 局部相互作用率”的链：

$$C(M,z) \to (R_s,\rho_s) \to \rho_{\mathrm{NFW}}(r) \to v_{\mathrm{disp}}(r),\,v_{\mathrm{vir}} \to \text{捕获率}$$

论文第一部分主要建立暗物质晕模型，包含四层内容：

1. NFW 密度剖面；
2. 浓度参数与晕质量的关系；
3. 暗物质晕质量函数；
4. 晕内 PBH 速度分布。

## 1. NFW 密度剖面

论文采用

$$\rho_{\mathrm{NFW}}(r)=\frac{\rho_s}{(r/R_s)(1+r/R_s)^2}.$$

定义无量纲半径

$$x=\frac{r}{R_s},$$

则

$$\rho(r)=\frac{\rho_s}{x(1+x)^2}.$$

这里：

- $R_s$：尺度半径；
- $\rho_s$：尺度密度；
- $r$：距离晕中心的物理半径。

两个极限

当 $r\ll R_s$ 时，

$$\rho(r)\propto r^{-1}.$$

因此中心有 cusp，但密度发散只有 $1/r$，不是更强的奇点。

当 $r\gg R_s$ 时，

$$\rho(r)\propto r^{-3}.$$

所以外部区域密度下降很快。

物理上，NFW 晕由“中心高密度、外部稀疏”的结构组成。这对 PBH 捕获非常重要，因为捕获率大致满足

$$\Gamma\propto n_{\mathrm{PBH}}^2\propto\rho_{\mathrm{NFW}}^2.$$

因此密度高的内区会显著增强相互作用。

## 2. 浓度参数 $C$

浓度定义为

$$C=\frac{R_{\mathrm{vir}}}{R_s}.$$

因此

$$R_s=\frac{R_{\mathrm{vir}}}{C}.$$

浓度越大，意味着相同的总晕质量被压缩在更小的尺度半径内，晕更加集中。

论文定义

$$g(C)=\ln(1+C)-\frac{C}{1+C}.$$

这个函数来自 NFW 质量积分。

## 3. NFW 总质量积分

晕内半径 $R$ 以内的质量为

$$M(<R)=4\pi\int_0^R r^2\rho_{\mathrm{NFW}}(r)\,dr.$$

代入 NFW 形式：

$$M(<R)=4\pi\rho_s R_s^3\int_0^{R/R_s}\frac{x}{(1+x)^2}\,dx.$$

积分结果为

$$M(<R)=4\pi\rho_s R_s^3\left[\ln(1+x)-\frac{x}{1+x}\right].$$

在 $R=R_{\mathrm{vir}}$ 时，$x=C$，因此

$$M_{\mathrm{vir}}=4\pi R_s^3\rho_s g(C).$$

这就是论文的质量归一化关系。

它可以反过来写成

$$\rho_s=\frac{M_{\mathrm{vir}}}{4\pi R_s^3 g(C)}.$$

这一步非常关键，因为之后所有局部密度都可以用 $M_{\mathrm{vir}}$、$R_s$、$C$ 表示。

## 4. 为什么有 $\delta_c$

论文写作

$$\rho_s=\rho_{\mathrm{crit}}(z)\delta_c,$$

其中

$$\delta_c=\frac{200C^3}{3g(C)}.$$

原因是 $R_{\mathrm{vir}}$ 被定义为平均密度达到

$$\bar\rho(<R_{\mathrm{vir}})=200\rho_{\mathrm{crit}}$$

的半径。

另一方面，

$$M_{\mathrm{vir}}=\frac{4\pi}{3}R_{\mathrm{vir}}^3(200\rho_{\mathrm{crit}}).$$

而 NFW 质量公式给出

$$M_{\mathrm{vir}}=4\pi R_s^3\rho_s g(C).$$

用 $R_{\mathrm{vir}}=CR_s$ 代入：

$$4\pi R_s^3\rho_s g(C)=\frac{4\pi}{3}R_s^3C^3(200\rho_{\mathrm{crit}}).$$

约去公共因子：

$$\rho_s=\rho_{\mathrm{crit}}\frac{200C^3}{3g(C)}.$$

因此

$$\delta_c=\frac{200C^3}{3g(C)}.$$

这不是额外的经验公式，而是 NFW 质量归一化与 virial 定义共同推出的结果。

## 5. $C(M,z)$：质量和红移依赖

浓度并不是常数，而是

$$C=C(M,z).$$

论文比较两个模型：

- Ludlow16；
- Prada12。

一般趋势是：

- 小质量晕浓度更高；
- 大质量晕浓度更低；
- 同一质量的晕在不同红移具有不同浓度。

为什么小质量晕浓度更高？

因为小晕通常更早形成，形成后有更长时间进行动力学演化；大晕往往较晚形成，因此更加松散。

浓度直接影响 PBH 捕获率。由

$$R_{\mathrm{halo}}\propto\frac{M_{\mathrm{vir}}^2}{R_s^3 g(C)^2}f(C),$$

并且 $R_s=R_{\mathrm{vir}}/C$，可以看出增大 $C$ 会显著增强中心密度和 $\rho^2$ 积分。

所以论文中 Prada12 在某些红移下给出更高捕获率，本质上是它预测了更高浓度。

## 6. 晕质量函数

单个晕的并合率还不够，因为宇宙中有不同质量的晕。

需要知道每单位体积中有多少质量为 $M$ 的晕：

$$\frac{dn}{dM}.$$

论文采用 Press–Schechter 类型的晕质量函数。标准结构可以写成

$$\frac{dn}{dM}=\frac{\rho_{m,0}}{M}\frac{d\ln\nu}{dM}f(\nu),$$

其中

$$\nu(M,z)=\frac{\delta_c(z)}{\sigma(M,z)}.$$

这里：

- $\rho_{m,0}$：今天的平均物质密度；
- $\sigma(M,z)$：质量尺度 $M$ 上的密度涨落；
- $\delta_c(z)$：形成晕所需的临界过密度；
- $\nu$：峰高参数。

Press–Schechter 乘法函数通常为

$$f(\nu)=\sqrt{\frac{2}{\pi}}\nu e^{-\nu^2/2}.$$

物理意义：

- $\nu$ 大：代表罕见的高密度峰；
- $\nu$ 小：代表更常见的低质量晕。

最终宇宙总并合率是

$$R(z)=\int dM\,R_{\mathrm{halo}}(M,z)\frac{dn}{dM}.$$

这一步把“单个晕的物理”转换成“整个宇宙的平均率”。

## 7. 晕内 PBH 速度分布

论文没有简单假设所有 PBH 具有同一个速度，而是使用截断 Maxwell 分布：

$$P(v)=F_0 v^2\left[e^{-v^2/v_{\mathrm{disp}}^2}-e^{-v_{\mathrm{vir}}^2/v_{\mathrm{disp}}^2}\right],$$

其中

$$0<v<v_{\mathrm{vir}}.$$

含义是：

- $v^2$：三维速度空间体积因子；
- $v_{\mathrm{disp}}$：速度弥散；
- $v_{\mathrm{vir}}$：逃逸/virial 速度尺度；
- $F_0$：归一化常数。

归一化条件为

$$4\pi\int_0^{v_{\mathrm{vir}}}v^2\left[e^{-v^2/v_{\mathrm{disp}}^2}-e^{-v_{\mathrm{vir}}^2/v_{\mathrm{disp}}^2}\right]dv=F_0^{-1}.$$

为什么必须考虑速度分布？

因为 GW 捕获截面满足

$$\sigma(v)\propto v^{-18/7}.$$

相对速度越小，捕获截面越大。因此低速区域对捕获率贡献特别重要。

## 8. $v_{\mathrm{disp}}$ 和 $v_{\mathrm{vir}}$

论文取最大圆速度位置

$$x_{\max}=\frac{r_{\max}}{R_s}=2.1626.$$

在该位置：

$$v_{\mathrm{disp}}=\sqrt{\frac{GM(<r_{\max})}{r_{\max}}}.$$

而 virial 速度为

$$v_{\mathrm{vir}}=\sqrt{\frac{GM_{\mathrm{vir}}}{R_{\mathrm{vir}}}}.$$

内部质量为

$$M(<r_{\max})=4\pi R_s^3\rho_s\left[\ln(1+x_{\max})-\frac{x_{\max}}{1+x_{\max}}\right].$$

直观上：

- $v_{\mathrm{disp}}$ 描述晕内 PBH 的典型随机运动；
- $v_{\mathrm{vir}}$ 描述整个晕的引力束缚速度尺度；
- 大质量晕通常速度更高；
- 高速度会压低 GW 捕获率。

因此存在竞争：

高密度 $\Rightarrow$ 增强捕获，
高速度 $\Rightarrow$ 抑制捕获.

这也是为什么并不是最大质量晕一定贡献最大并合率。

## 9. 第一部分最重要的物理图像

可以把一个 PBH 晕想成三张叠加的图：

**密度图**

$$\rho(r)\sim\begin{cases}r^{-1}, & r\ll R_s,\\ r^{-3}, & r\gg R_s.\end{cases}$$

中心密度高，外围密度低。

**速度图**

小晕通常速度低，大晕通常速度高。

**捕获图**

捕获率局部满足

$$d\Gamma\sim\rho^2(r)\langle v\sigma(v)\rangle d^3r.$$

由于

$$\sigma(v)\propto v^{-18/7},$$

捕获率同时偏好：

- 高密度；
- 低速度。

论文之后的所有数值结果，都是这两个效应与晕质量函数、浓度演化共同作用的结果。

## 10. 建议你掌握的四个检查题

你可以先尝试回答：

1. 为什么 $\rho_s$ 不是自由参数，而可以由 $M_{\mathrm{vir}},R_s,C$ 确定？
2. 为什么捕获率中的径向积分是 $\int r^2\rho^2 dr$，而不是 $\int r^2\rho dr$？
3. 为什么更高的浓度通常提高捕获率？
4. 为什么大质量晕虽然总质量大，却不一定有更高的 PBH 捕获率？

答案分别是：

1. 因为 NFW 质量归一化条件固定了 $\rho_s$；
2. 捕获需要两个 PBH，局部反应率正比于 $n_1n_2$；
3. 高浓度使 $R_s$ 更小、中心密度更高；
4. 大质量晕的速度弥散更高，而捕获截面对速度非常敏感，$\sigma\propto v^{-18/7}$。

下一步最适合继续学习的是：从 Eq. (11) 推导 Eq. (15)，也就是完整理解 GW 捕获截面如何经过速度平均和 NFW 径向积分变成每个暗物质晕的并合率。
